name: Setup numba-dppy
description: 'Setup conda environment, build numba-dppy and setup devices'
runs:
  using: "composite"
  steps:
    - name: Setup conda
      run: echo $CONDA/bin >> $GITHUB_PATH
      shell: bash

    - name: Setup conda environment
      run: conda env update -n base -f environment/docs.yml --prune
      shell: bash

    - name: Build numba-dppy
      run: |
        export PATH=$CONDA/bin-llvm:$PATH
        python setup.py develop
      shell: bash

    - name: Setup OpenCL CPU device
      run: echo "OCL_ICD_FILENAMES=libintelocl.so" >> $GITHUB_ENV
      shell: bash
